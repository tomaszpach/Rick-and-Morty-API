import React from 'react';
import InputsForm from './form';

const Inputs = () => {
    return (
        <div>
            <InputsForm />

            <button>calculate</button>
        </div>
    );
};

export default Inputs;
